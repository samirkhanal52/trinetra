[metadata]
# This file is intentionally left minimal.
# All project metadata is defined in pyproject.toml as per PEP 621.
# This file exists solely to enable robust editable installations
# with older versions of pip and setuptools.
name = trinetra

[options]
packages = find_namespace:
package_dir =
    = src

[options.packages.find]
where = src
